-- ============================================================
--  ПРИЧИНА СТАТУСА БРОНИ
--  Применяется поверх add-booking-status.sql.
--  status_reason — почему гость не пришёл (no_show) или бронь
--  отменена (cancelled). Доступна офикам и админам.
--  Обратная совместимость: старые строки = null.
-- ============================================================

alter table public.bookings
  add column if not exists status_reason text;
